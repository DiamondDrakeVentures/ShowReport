// Package encoding encodes and decodes CSV file to and from struct.
// Internally, this package uses encoding/csv.
//
// Struct fields are mapped with the `csv` struct tag.
// This package also supports time formatting with the `time` struct tag.
package encoding
